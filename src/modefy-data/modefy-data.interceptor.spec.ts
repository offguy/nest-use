import { ModefyDataInterceptor } from './modefy-data.interceptor';

describe('ModefyDataInterceptor', () => {
  it('should be defined', () => {
    expect(new ModefyDataInterceptor()).toBeDefined();
  });
});
